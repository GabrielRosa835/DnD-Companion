package dnd_companion.common.design_patterns;

public interface ChainOfResponsability {
	/*
	 * Chains multiple code blocks called "handlers" against a single original object
	 * Handlers can change witch handler will be the next one
	 */
}
